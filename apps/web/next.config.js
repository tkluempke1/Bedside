/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    appDir: true,
  },
  compiler: {
    // Enable styled components or other compiler options here if necessary
  }
};

module.exports = nextConfig;
